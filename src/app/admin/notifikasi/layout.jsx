export const metadata = {
    title: "Notifikasi Admin",
    description: "Kelola notifikasi dan broadcast sistem Namura Property.",
    robots: { index: false, follow: false },
};

export default function NotifikasiAdminLayout({ children }) {
    return children;
}
