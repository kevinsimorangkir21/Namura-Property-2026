export const metadata = {
    title: "Kelola User",
    description: "Kelola akun admin dan marketing Namura Property.",
    robots: { index: false, follow: false },
};

export default function KelolaUserLayout({ children }) {
    return children;
}
