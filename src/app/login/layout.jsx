export const metadata = {
    title: "Login",
    description: "Masuk ke dashboard admin Namura Property.",
    robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
    return children;
}
